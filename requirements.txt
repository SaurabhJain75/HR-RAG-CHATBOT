# ══════════════════════════════════════════════════════════════════════════════
# HR Policy RAG Chatbot — Dependencies
# torch is NOT listed here — installed automatically by sentence-transformers
# CPU-only torch is forced via -f flag in Dockerfile
# ══════════════════════════════════════════════════════════════════════════════

# LLM — Groq via OpenAI-compatible SDK
openai>=1.30.0

# Embeddings — sentence-transformers pulls correct torch version automatically
sentence-transformers>=2.7.0

# Vector Store
chromadb>=0.5.0

# Document Loaders
pypdf>=4.2.0
python-docx>=1.1.0

# Config & Validation
python-dotenv>=1.0.0
pydantic>=2.7.0

# FastAPI stack
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Streamlit UI
streamlit>=1.35.0

# Utilities
numpy>=1.26.0
